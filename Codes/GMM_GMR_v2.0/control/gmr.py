import os, sys

def gmr(Priors, Mu, Sigma, x, inputs, outputs):
	print Priors, Mu, Sigma, x, inputs, outputs
	joints = []
	
	#input ball position as one data point
	nbData = 1
	nbVar = len(Mu[0])

	return joints
